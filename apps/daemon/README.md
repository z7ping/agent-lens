# AgentLens 1.0 Daemon

This is the clean-rebuild process entrypoint for AgentLens 1.0.

It boots `@agent-lens/runtime-cordis` directly. Prototype / 0.x `server/` code is intentionally not part of this runtime path.
