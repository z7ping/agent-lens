# AgentLens Logo 候选方案存档

本目录用于保存 2026-08-25 Logo 设计讨论中的候选方向。

说明：最终采用的第 5 套存在正式 SVG 源，因此 `concept-05-final.svg` 与正式设计源一致；其余候选当时仅作为生成图讨论，没有独立矢量源进入仓库，本目录按当天“极简、轨迹 / 路径”方向重构为 SVG，用于保留设计演进，不宣称与当时位图逐像素一致。

| 文件 | 方向 | 状态 |
| --- | --- | --- |
| `concept-01.svg` | 单轨迹进入镜头焦点 | 候选重构 |
| `concept-02.svg` | 双来源路径汇聚后进入镜头 | 候选重构 |
| `concept-03.svg` | 几何字母 / 路径与镜头结合 | 候选重构 |
| `concept-04.svg` | 环形观察域 + 穿行轨迹 | 候选重构 |
| `concept-05-final.svg` | 双来源节点 → 双轨迹 → 双层镜头焦点 | **最终采用** |
| `concept-06.svg` | 平行路径汇聚到双层观察镜头 | 候选重构 |

最终正式 Logo 仍以父目录 `agentlens-logo.svg` 和 `agentlens-logo-small.svg` 为唯一设计基线；本目录不被构建流程直接引用。
