# AgentLens 设计基线

当前状态：**1.0 稳定化 / v2.1 表现层收敛**。

## 唯一当前高保真原型

- 入口：`docs/design/mockups/index.html`
- 当前画板：`docs/design/mockups/v2/index.html`
- 当前画板目录：`docs/design/mockups/v2/`

`mockups/index.html` 只负责跳转，不再承载独立版本。

以下历史原型已经删除，不再作为实现、验收或文档引用依据：

- 根目录旧 `review.html`
- 根目录旧 `tools.html`
- 根目录旧 `agents.html`
- 根目录旧 `states.html`
- 根目录旧 `system.html`
- 根目录旧 `backup.html`
- 根目录旧 `insights.html`
- `review-calm.html`
- 与上述页面绑定的旧 `mockups/assets/`

## 对齐原则

正式 Web 以 v2.1 当前稿的布局、信息层级、交互密度、字号、间距和状态表达为表现层基线，但不能为了像原型而牺牲事实真实性。

当正式能力比静态稿更完整时，以真实产品语义为准，并同步更新原型。例如：

- 智能体必须区分“已检测”和“已启用采集”；
- 可观测能力可以展示采集方式和不完整原因；
- 工具错误示例如果当前接口没有逐次错误载荷，不得用演示文本冒充真实错误；
- 未报告的生命周期状态保持“未报告 / —”，不能按 0 展示；
- 健康接口没有端口字段时，顶部状态不得硬编码端口。

## 当前正式 Web 已收口

- 五个一级页面直达；
- 任务复盘左右独立滚动、时间分组、智能折叠、轮次导航；
- 用户消息右侧、智能体消息左侧；
- Evidence（证据）默认可见；
- 复盘检查器“详情 / 证据 / 原始数据”三页签；
- 搜索防抖和筛选地址状态化；
- 工具分析真实会话钻取、Top 3 会话分布、“需要关注”和能力资产比例条；
- 智能体来源列表 + 单一详情、真实采集启用状态、能力采集说明；
- 高频资产真实比例条、技能生命周期真实比例漏斗；
- 顶部后台服务 / 实时通道合并状态入口；
- v2.1 最终对齐样式由 `packages/web/src/v2-alignment.css` 最后加载。

## 尚需继续验收

后续仍按当前 v2.1 逐页做真实页面验收，重点检查：

1. 任务复盘的“显示原始记录”审计模式与原型交互是否需要补齐；
2. 使用洞察与资产备份的真实页面细节；
3. 空状态 / 错误态 / 首次运行引导；
4. 明暗主题、窄屏响应式与逐像素间距；
5. 原型因新真实语义产生的反向更新。
